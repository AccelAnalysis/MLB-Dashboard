# Data Layer

Phase 2 introduces a home for static configuration and future seed data modules.

Current module:

- `constants.js` — product categories, board columns, regions, modal tabs, status key, and UI storage key.

Future production work should move demo projects and seed data out of `src/MLBDashboard_field_complete.jsx` only after the current behavior is validated against the imported constants and utility modules.
